var Sent1 = ('This is the beginning of the string') //Define variable Sent1
var Sent2 = (' and this is the end of the string')   //Define variable Sent2

var A = ("\"Waddup\" World,"                        //Beginning of defining variables, starting with A
        +" again?!"), B=(Sent1 + Sent2), C=(3+3)      //Defining Variables B and C

window.alert(A);                                    //Invoking window.alert function to display variable in a popup window
document.write(B);                                  //Invoking document.write function to transcribe variable onto HTML page